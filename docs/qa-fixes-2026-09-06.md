# 测试问题修复与回归结果

日期：2026-09-06。对应 `docs/qa-report-2026-09-06.md` 的 D1-D6、T1 均已修复，变更基于 `43b181e`。后续反馈中的报告链接跳转问题也已修复。

## 修复清单

| 问题 | 修复结果 | 回归证据 |
| --- | --- | --- |
| D1 自定义端口误拒绝同源请求 | Origin 校验从真实监听地址读取端口，仅补充该端口的本地 HTTP 来源；不信任 Host/转发头，不放开任意外部来源 | 随机端口的 POST、预检、SSE 和恶意来源测试；3181 生产页面完整新建会话成功 |
| D2 当前目录会话无法推送 | 从执行目录解析实际分支，使用完整分支 ref 推送，拒绝分离 HEAD | 本地 bare 远端验证普通会话、Worktree、分支切换和分离 HEAD |
| D3 未提交修改仍被合并归档 | 合并前检查源 Worktree，拒绝未提交修改并保留活动会话 | 未暂存、已暂存、未跟踪文件均返回 409；目标提交和会话生命周期不变；正常合并通过 |
| D4 文件被当作执行目录 | 新建、已有目录创建、重绑复用目录存在性和类型校验；重绑空路径不再回退到进程目录 | 普通文件、缺失目录、空路径被拒绝，原目录不变且无新增会话 |
| D5 非法参数返回 500 | Worktree 名称及分支字符校验、未知 Provider 返回 400；共享协议增加工作区部分更新 schema | 原 3 条失败用例通过；合法部分更新保持其他字段，非法类型/超长名称不写库 |
| D6 建议浮层遮住错误 | 提交时取消定时/在途目录搜索、清理加载状态、关闭建议列表 | 桌面和移动端 E2E 使用真实目录建议与模拟提交失败，断言错误中心命中提示本身 |
| T1 单测污染真实仓库 | Worktree 单测改用独立临时 Git 仓库，上传/Relay/Worktree 全部隔离并由退出钩子清理 | 完整测试前后 `git worktree list` 和所有本地分支引用完全一致 |
| 报告链接跳出页面 | 兼容 `/D:/code/...` 形式的 Windows 绝对路径，仍保留工作区边界检查 | 桌面和移动端分别点击测试计划、完整报告，文件抽屉显示内容且没有新页面或跳转 |
| 构建后新资源被返回为 HTML | 静态资源未命中启动时的路由时，按文件路径读取；不存在的资源返回 404 | 服务启动后新增 JS 文件仍能读取，已运行服务的最新入口 JS 返回正确类型 |

## 验证结果

| 命令或检查 | 结果 |
| --- | --- |
| `pnpm build` | 通过 |
| `pnpm test` | 172 通过、0 失败、2 跳过 |
| `pnpm test:e2e` | 5 通过、0 失败 |
| `node --test scripts/qa/dev-smoke.test.mjs` | 2 通过、0 失败 |
| `pnpm release:check:imports` | 通过 |
| `git diff --check` | 通过 |
| 真实仓库分支和 Worktree 前后比较 | 完全一致，没有新测试遗留 |

单元/集成统计：shared 2、relay 9、protocol 4、daemon 112、web 45 通过。原始 14 条接口补充用例已经进入默认 daemon 测试，并补充了 5 条边界用例、2 条 CORS 单测、1 条构建资源测试和 2 条链接识别测试。

5 条 E2E 均使用实际随机监听端口，不再通过显式注入该端口白名单绕开默认启动行为。新增错误提示和报告链接用例分别检查 `1440x900` 和 `390x844`；开发冒烟另外覆盖 `1920x1080`、`768x1024`、`320x568`。

## 证据与运行

- 日志：`tmp/fix-20260906/`。
- 报告链接回归截图：`tmp/report-links/screenshots/`。
- 修复截图：`tmp/fix-20260906/screenshots/create-error-visible-1440.png`、`create-error-visible-390.png`。
- 自定义端口新建成功截图：`tmp/fix-20260906/screenshots/production-custom-port.png`。
- 开发预览：`http://127.0.0.1:5184`；Daemon 和生产页面：`http://127.0.0.1:3181`。
- 预览使用独立的 `%TEMP%/promptx-fix-20260906-dev` 数据目录；日常服务和数据未改动。

## 仍有的验证限制

两项原有跳过分别是 Windows 普通符号链接权限和旧非 Git 目录用例；新增 junction 和非 Git 目录 API 用例已通过。真实在线模型、公网 Relay、Android 真机及服务启停组合命令的既有未验证项保持原报告说明。本轮没有把环境限制、占位 lint 和依赖数据库老化警告归类为已修复的业务缺陷。
